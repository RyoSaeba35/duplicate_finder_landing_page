import type { LegalContent } from "../legalTypes";
import en from "./en";

const pt: LegalContent = {
  ...en,
  backHome: "← Voltar ao início",
  authoritativeNotice:
    "Este documento está disponível apenas em inglês. Em caso de ambiguidade ou erro de tradução, prevalece a versão em inglês.",
};

export default pt;
