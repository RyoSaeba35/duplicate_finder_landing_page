import type { LegalContent } from "../legalTypes";
import en from "./en";

const ja: LegalContent = {
  ...en,
  backHome: "← ホームに戻る",
  authoritativeNotice:
    "この文書は英語版のみ提供されています。内容に曖昧な点や翻訳の誤りがある場合は、英語版が優先されます。",
};

export default ja;
