import type { LegalContent } from "../legalTypes";
import en from "./en";

const pl: LegalContent = {
  ...en,
  backHome: "← Powrót do strony głównej",
  authoritativeNotice:
    "Ten dokument jest dostępny tylko w języku angielskim. W przypadku niejasności lub błędów tłumaczenia obowiązuje wersja angielska.",
};

export default pl;
