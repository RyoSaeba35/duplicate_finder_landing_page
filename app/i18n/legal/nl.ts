import type { LegalContent } from "../legalTypes";
import en from "./en";

const nl: LegalContent = {
  ...en,
  backHome: "← Terug naar home",
  authoritativeNotice:
    "Dit document is alleen in het Engels beschikbaar. Bij onduidelijkheid of vertaalfouten is de Engelse versie doorslaggevend.",
};

export default nl;
