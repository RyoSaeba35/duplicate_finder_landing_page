import type { LegalContent } from "../legalTypes";
import en from "./en";

const de: LegalContent = {
  ...en,
  backHome: "← Zurück zur Startseite",
  authoritativeNotice:
    "Dieses Dokument ist nur auf Englisch verfügbar. Bei Unklarheiten oder Übersetzungsfehlern gilt die englische Fassung.",
};

export default de;
