import type { LegalContent } from "../legalTypes";
import en from "./en";

const es: LegalContent = {
  ...en,
  backHome: "← Volver al inicio",
  authoritativeNotice:
    "Este documento solo está disponible en inglés. En caso de ambigüedad o error de traducción, prevalece la versión en inglés.",
};

export default es;
